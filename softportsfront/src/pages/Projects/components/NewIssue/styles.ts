import { Flex } from "antd";
import styled from "styled-components";

export const FieldsBox = styled(Flex)`
    gap: 10px;
    width: 50%;
    flex-direction: column;
    justify-content: space-between;
`